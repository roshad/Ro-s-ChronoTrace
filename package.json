{
  "name": "ros-chronotrace",
  "version": "1.3.0",
  "description": "Desktop time-tracking application with automated context capture",
  "type": "module",
  "scripts": {
    "dev": "npm run slint:dev",
    "build": "npm run slint:build",
    "web:dev": "vite",
    "web:build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev --features legacy-tauri",
    "tauri:build": "tauri build",
    "version:sync": "node ./scripts/sync-version.mjs",
    "version": "npm run version:sync && git add src-tauri/tauri.conf.json src-tauri/Cargo.toml",
    "release:patch": "node ./scripts/release.mjs patch",
    "release:minor": "node ./scripts/release.mjs minor",
    "release:major": "node ./scripts/release.mjs major",
    "release:version": "node ./scripts/release.mjs",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "slint": "cargo run --manifest-path src-tauri/Cargo.toml --bin slint-app --no-default-features",
    "slint:dev": "cargo run --manifest-path src-tauri/Cargo.toml --bin slint-app --no-default-features",
    "slint:check": "cargo check --manifest-path src-tauri/Cargo.toml --bin slint-app --no-default-features",
    "slint:build": "cargo build --manifest-path src-tauri/Cargo.toml --bin slint-app --release --no-default-features",
    "check": "npm run slint:check"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.59.20",
    "@tauri-apps/api": "2.9.1",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "@tauri-apps/plugin-updater": "^2.9.0",
    "lucide-react": "^0.454.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "styled-components": "^6.3.8",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.6",
    "@babel/preset-react": "^7.28.5",
    "@tauri-apps/cli": "^2.1.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/jest": "^30.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "babel-jest": "^30.2.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "~5.6.2",
    "vite": "^5.4.11"
  }
}
