<!-- maintain-project-plan: enabled -->

> 状态约定：`[ ]` 表示未完成，`[x]` 表示已完成。只有确认需求已完成后才标记为 `[x]`。
- [x] **摧毁webview**时保持后台进程来截图
	- 窗口失去焦点1分钟后，自动摧毁webview
	- 显示图标在系统托盘，可通过该图标退出后台进程。
	- 窗口可主动关闭，不影响后台进程。
	- [x] 窗口再次创建时，维持上次的状态(最大化，时间轴的滚动条位置等)
	- [x] 可在设置中关闭“失焦 1 分钟后自动销毁 WebView”，关闭后窗口保持常驻
- [x] **统计**时间范围内，各类行为的时间占比和总时间



- [x] **窗口活动捕获**: 每分钟记录当前活动窗口标题和进程名
- [x] **数据导出**: 导出所有数据为 JSON 格式
- [x] **日期导航**: 轻松浏览不同日期的时间记录
- [x] 操作提示，比如快捷键等，放入问号图标呼出的提示框中
- [x] 默认暗色
- [x] 支持检测更新，可手动更新，更新失败给出轻提示。
- [x] Ctrl + 鼠标滚轮 支持缩放界面

# 自动截图
    - [x] 每5分钟自动捕获屏幕截图（后台运行），应用启动后1分钟开始首次截图
    - [x] 截图时间点在时间轴上以小圆点标记，鼠标悬停显示tooltip 查看对应时间点截图；移入 tooltip 自动冻结，移出恢复跟随；左键点击 tooltip 内图片打开截图
    - [x] 默认webp格式，横向长度960，质量40，50kb以下。
    - [x] 允许用户设置截图大小、质量、存储位置
    - [x]  支持从设置界面打开截图存储文件夹。
# 主界面
- [x] **搜索**: 搜索指定时间范围的行为
    - [x] 搜索结果显示在统计上方
    - [x] 时间范围可以调节，比如当日、本月、本年等
    - [x] 能保存一些搜索项目，实时显示当前时间范围内的命中条数及相关条目的总时长。
## 当前行为操作区
- [x] 可输入要执行的行为，并点击开始。
- [x] 可随时更改标签
- [x] 可删除当前行为
- [x] 分类选项直接横向显示在“你正在做什么？”输入框旁，不通过“分类”按钮展开二级菜单

## 横向时间轴
- [x] 切换日期时，时间轴滚动条位置不变。
- [x] hover 显示区间、截图、进程统计、标签、类别
	- [x] 区间后显示持续时间
	- [x] 不可盖住时间轴
- [x] 时间轴占据100vw
- [x] 支持缩放和滚动，最大视野24小时，最小4小时
	- [x] 启动时，默认维持上次的缩放倍率。
- [x] 进程条，详见进程条.md
	- [x] 按秒采样前台进程（仅进程名）并按天查询
	- [x] 时间轴状态条显示进程区段，hover 展示 Top 3 进程耗时
	- [x] 点击状态条空白区可创建对应时间条目
		- [x] 本条目延迟一秒，避免重叠
		- [x] 创建条目时点击开始，进行的动作是：创建条目，并且继续开始当前条目，不是另起条目从零计时。
	- [x] 启动和每小时清理 30 天前采样数据
### 行为条
- [x] 点击行为条弹窗提供“再开”，从当前时间新建并开始相同内容、类型和所选行为的行为条目
- [x] 时间轴上标签的可多行显示，横向满后另起一行
- [x] 条目时间精确到秒
- [x] **拖拽**:
	- [x] 在时间轴上拖拽选择时间范围，快速创建时间条目
	- [x] 拖拽后出现的编辑框，复用TimeEntryDialog
	- [x] 已创建的条目，也支持拖拽更改范围。
- [x] 点击打开的条目**编辑窗**
	- [x] 有重新开始按钮，默认将空隙时间全部纳入当前条目，并继续计时。
		- [x] 最后的行为或进程条区块才显示。
		- [x] 重新开始的行为，当前持续时间显示行为总长
	- [x] 支持输入更改时间范围
	- [x] 类别不显示在二级菜单，直接在编辑窗中选择类别
- [x] 支持条目分类
	- [x] 所有类别横向排列，以使用户能在一行中点到所有类别，不用滚动页面。
	- [x] 每个条目可以选择一个行为类别，类别可选择对应的颜色。
	- [x] 类别本身可以修改（支持重命名、删除）
		- [x] 修改菜单在设置中，行为条上不可修改，以免频繁误操作。
- [x] 当前条目
	- [x] 时间轴上随计时增长
	- [x] 当前行为未结束，关闭应用后再次打开继续计时。



# Bugfix:
- 😭：花了比较多力气
- [x] 260804 时间轴滚动位置在软件退出或 WebView 销毁重建后恢复到最左：持久化最后有效位置，并在布局稳定后恢复；验证退出重启、失焦超过 1 分钟重建和窗口尺寸变化。
- [x] 😭 260223 github 自动更新 powershell""展开了$变量导致密码错误。
- [x] 260221 启动时显示白屏，过几秒才有画面
- [x] 260213 删除当前正在进行的行为后console报错`TimelineView.tsx:252  Failed to update running timer entry: Failed to fetch time entry: Query returned no rows`
- [x] 260213 关键字"药"搜不到"吃药"
    - 修改行为类别为无类别失败：console报错：Time entry overlaps with an existing entry
- [x] 时间占比统计不实时更新，切换时间范围后才更新
- [x] 260212 行为条颜色与类别颜色不一致（修复后行为条颜色与类别颜色一致）
    - 行为条文字标签溢出
- [x] hover出现后原地点击行为条，仍可打开行为编辑；双击不再选中tooltip文字（修复悬浮窗点击遮挡）
    - 😭在已有行为条之上点击，还是会出现类似空白处点击的拖动框的虚影。换了几种表述修好的。
- [x] 260211 hover在空白区域，也有hover窗口
- [x] 260211 自动截图后，截图点不立即显示在时间轴上，需要切换日期才显示

# 开发辅助
- [x] 提供可双击启动开发项目的 Windows 批处理脚本

# 托盘菜单

> 范围：只实现右键打开的托盘统计面板；不实现悬停、Ctrl 触发、托盘图标显示时间或其他入口。

- [x] 右键托盘即时打开原生 Win32 统计面板
	- [x] 使用 Rust + Windows API/GDI 绘制，不创建额外 WebView
	- [x] 使用分类颜色、图标和分区布局，保留打开主窗口和退出操作
	- [x] 计时或分类数据变化后更新面板内容
- [x] 当前行为托盘状态与动态类别图标
	- [x] 顶部显示当前行为、类别和已执行时长，不显示今日总时长
	- [x] 没有行为时显示空闲提示并保留分类明细
	- [x] 托盘图标按当前类别染色，未分类使用中性灰，无行为恢复原始图标
	- [x] 前端 activeTimer 同步到原生托盘，并通过前后端回归测试
- [x] 活动行为在托盘面板打开时持续计入分类累计时长

# v1.6.0 发布

- [x] 完成本地 v1.6.0 发布准备
	- [x] 更新变更日志和所有版本来源
	- [x] 通过发布前验证并确认版本一致
- [ ] 完成 GitHub v1.6.0 远程发布
	- [ ] 推送版本标签并确认 Actions 发布成功
	- [ ] 验证 Release、安装包和自动更新资源
